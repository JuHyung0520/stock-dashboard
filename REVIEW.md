# 프론트엔드 전체 검토 (2026-08-19)

> **2026-08-19 전체 적용 완료.** 아래 항목 전부 반영됨 — tokens.css 신설(색·타입 8단계·모션 2종 통합),
> 버그 4건 수정, 대비 3.03→5.2, 폰트 바닥선 10.8px, 지수바 1줄 티커화, 선택 종목가 27.75px 주인공화,
> /flow 스캐너 최상단 이동+강도순 정렬, 스크러버 클릭 고정, 삭제 undo 토스트, /flow 갱신 상태 표시,
> 사이드바 clamp+컨테이너 쿼리, 매트릭스 트랙 바닥(minmax 24px), 터치 대응. 적용 후 실측 재검증 통과.

4개 렌즈(정보 위계 / 가시성·타이포 / 반응형 / 상호작용)로 검토. 실측 기반 — 대비·크기·좌표는 전부 브라우저에서 계산한 값.

## 우선순위 1 — 실제 버그 (즉시 고칠 것)

| # | 문제 | 위치 | 수정 |
|---|---|---|---|
| 1 | 타임 스크러버 패널이 마우스를 떼면 사라짐 — 값을 읽으러 내려가는 동작이 패널을 지움 | flow.js:260 | 클릭으로 고정(pinned), Esc/바깥클릭 해제 |
| 2 | 실패한 탭 재클릭해도 재시도 안 됨 (얼리 리턴). 호가는 폴링 실패 시 영구 정지 | app.js:449 | 같은 탭 재클릭 허용 + "다시 시도" 버튼 |
| 3 | 검색 키보드 조작 미구현 — `.search-item.active`는 죽은 CSS | app.js:879 | ↑↓ + Enter 구현 |
| 4 | flow.css 760px 이하에서 `html{font-size:14px}` — 좁을수록 글씨가 작아짐 | flow.css:305 | 삭제 |

## 우선순위 2 — 가시성 (사용자가 제기한 문제의 핵심)

- **`--text-faint` 3.03:1 → `#808a9c`(5.2:1), `--text-dim` → `#a3adbd`(8.0:1)**. 40개 셀렉터가 쓰는 토큰 2개만 바꾸면 됨.
- **폰트 바닥선 0.72rem(10.8px)** — 0.6~0.66rem(9.0~9.9px) 21개 셀렉터 전부 올림.
- **opacity로 흐리게 한 것들** — 특히 `.stat-card.muted`(결측 알림)가 2.48:1로 페이지에서 제일 안 보임. opacity 대신 dashed 테두리/빗금.
- 검색창 포커스 표시 1.51:1 → accent 링으로.
- 음수 letter-spacing이 tabular-nums 무력화 (.m-date 등) → 제거.

## 우선순위 3 — 정보 위계

- **타이포 위계 부재**: 최대 글자가 코스피 지수(18.75px). 선택 종목 현재가를 1.85rem/800으로 올려 주인공으로, 지수는 1rem으로 낮춤.
- **첫 화면 예산**: 헤더+지수바 151px(21%) → 지수바를 1줄 티커로 눕혀 호가창을 fold 위로.
- **/flow 순서**: 유일한 '내 종목' 섹션(스캐너)이 맨 아래 → 타임라인 앞으로 이동 + 강도순 정렬.
- 기준선 카드의 전일/5일평/20일평 3줄은 매트릭스 hover와 중복 → "20일평 대비 N배" 한 줄로 압축.
- 종목상세 탭 선택을 localStorage에 기억 (지금은 종목 바꿀 때마다 호가로 리셋).
- 뉴스 패널 제목이 랭킹 탭에서도 '뉴스' → 탭에 따라 제목 변경.

## 우선순위 4 — 일관성

- **토큰 이원화**: 같은 개념이 두 파일에서 다른 이름·값 (--panel #12161f vs #131822 등) → `tokens.css` 신설로 통합.
- **폰트 31단계 → 8단계 스케일** (--fs-2xs 0.72rem ~ --fs-2xl).
- **트랜지션 6종 → 2종** (--t-fast 0.12s, --t-bar 0.3s).
- 탭·토글 13개에 hover/focus 없음 → 공통 hover 추가.
- 삭제 버튼 22×19px → 26×26px + 실행취소(또는 최소 undo 토스트).
- /flow에 갱신 상태 점(statusDot) 추가 — 실패 시 낡은 숫자가 그대로 남는 문제.

## 반응형 (16개 폭 320~1440px 실측)

가로 넘침은 전 구간에서 없음(검증됨). 진짜 문제는 넘침이 아니라 **넘쳐야 할 것이 조용히 뭉개지는 것**:

- **[높음] 랭킹 종목명 열이 46px로 붕괴** — 사이드바가 항상 400px이라 뷰포트 기준 620px 미디어쿼리가 영원히 발동 안 함. 5자 이상 종목명 전부 말줄임. → 컨테이너 쿼리(`.col-side { container-type: inline-size }`)로 전환.
- **[중간] 981~1200px: 사이드바 400px 고정이 메인을 갉아먹음** (1100px에서 메인 644px, 1000px에서 544px) + 980px에서 절벽. → `clamp(300px, 28%, 400px)` + 1180px 중간 단계.
- **[중간] /flow 매트릭스 min-width:640이 잘못된 축을 보호** — 날짜 칸이 44→15px로 뭉개진 *뒤에야* 바닥에 닿아 스크롤바가 안 생김. → 트랙에 바닥: `repeat(N, minmax(24px, 1fr))`, min-width 삭제 (검증됨).
- **[중간] ≤980px 스택 모드에서 동선 붕괴** — 관심종목 표가 높이 무제한(40종목이면 1,604px)이라 종목 선택→호가 확인이 1,600px 스크롤. 뉴스는 폭이 2배 됐는데 max-height는 오히려 340px로 줄음. → 표에 60vh 상한, 뉴스 480px.
- **[중간] 폰에서 삭제 버튼 물리적 도달 불가** — hover 전용(opacity:0) + 표 고정폭 502px이라 화면(375px) 밖. → `@media (hover:none) { opacity:1 }` + 거래량 열 숨김.
- [낮음] auto-fit 카드 고아(768~900px에서 4+1 배열), body max-width 불일치(1440 vs 1320), 검사 로그만 카드 밖.

※ 위계 렌즈의 "/flow 스캐너 1,874px = 페이지 36%" 수치는 오염된 측정에서 나온 것 — 실제는 263px. 다만 "내 종목 섹션을 위로 + 강도순 정렬" 제안 자체는 여전히 타당.

## 건드리지 말 것 (검토에서 확인된 강점)

- 상승 빨강/하락 파랑 — 접근성 관점에서도 최적 (적록색맹 안전). 부호(+/−) 이중 부호화 일관됨.
- 2026-08-18 레이아웃 결정(매매용 왼쪽/참고용 오른쪽), 뉴스 max-height, /flow 히어로 구조.
- 매트릭스 막대 인코딩, 주 단위 여백, 날짜 4칸 표기.
- 레이스 가드, justDragged, document.hidden 폴링 정지, 원인 말해주는 오류 문구.
- 로딩/오류 문구 통일("불러오는 중…", "~하지 못했어요"), 두 페이지의 숫자 서체 구분(의도된 것).
