# 토스증권 Open API 키 — 토스증권 PC웹(WTS) → 설정 → Open API → "Open API Key 설정"에서 발급
# 이 파일을 복사해서 .env 로 만들고 값을 채우면 됨:  cp .env.example .env
#
# 비워두면 서버가 자동으로 네이버(비공식) 수급 데이터로 폴백하니, 키 없이도 대시보드는 그대로 돌아감.
# ⚠️ 발급 화면 하단 "허용 IP 관리"에 이 컴퓨터의 공인 IP를 등록해야 함. 안 하면 전부 403.

TOSS_CLIENT_ID=
TOSS_CLIENT_SECRET=
