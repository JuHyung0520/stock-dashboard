# 로드맵 — raoni.xyz 기능 전수 검토 (2026-08-20)

> 개인용·비배포. 기능은 참고하되 코드·디자인은 직접 구현. 포트폴리오 겸용.

## 기능 인벤토리 (전수 조사 결과)

### 이미 우리에게 있는 것 ✅

| raoni 기능 | 우리 구현 | 비고 |
|---|---|---|
| 김프 (HL 선물 × 업비트 환율 → 괴리) | 대시보드 사이드바 패널 | 펀딩비까지 표시 |
| 평단 손익계산기 (차수·가중평단) | `/assets` | 미국 종목도 지원 (걔넨 국내만) |
| 매도 거래세 (선택 + ETF 자동 면제) | `/assets` | 동일 |
| 실현손익·수익률·APR | `/assets` | 동일 |
| 5초 실시간 갱신 | 전 화면 | 동일 |
| NXT 장전·장후 시세 | 관심종목·김프·assets | 동일 |
| ☁️ 서버 동기화 | **의도적 제외** | 우리는 localStorage + JSON 백업 (프라이버시) |

### 걔네한테 없는 우리 것 (포트폴리오 차별점)

호가창(장중 2초) · 세력 좌표(수급 분석 페이지 전체) · 공매도/프로그램/신용/대차 · 자동 마감요약(launchd) · 미국 종목 · 카드뷰(시총·목표가·52주) · 뉴스·랭킹

### 사이트가 확장됨 (2026-08-21 재조사)

raoni.xyz가 탭 12개로 커졌다. 확인된 경로: `/terminal` `/idx` `/marketcap` `/ram` `/adr` `/flow` `/calc` `/etf`
(탭 이름: 내자산·대시보드·지수·ETF·괴리율·시총비교·전고대비·터미널·램값·하닉ADR·수급·더보기)

**핵심 발견 — 바이낸스 토큰화 주식**: `fapi.binance.com/fapi/v1/ticker/24hr` 에 한국주식 perp이 있다.
`SAMSUNGUSDT` `SKHYNIXUSDT` `SKHYUSDT`(ADR) `DRAMUSDT` `MUUSDT` `SNDKUSDT` `NVDAUSDT` `TSMUSDT`.
24시간 거래 + klines 제공 → **램값(DRAM)·글로벌 반도체·24h 차트가 전부 여기서 나온다.**
하이닉스 ADR은 네이버 worldstock `SKHY.O` 로도 조회된다.
엔/달러는 직접 API가 없어 `USDKRW ÷ (JPYKRW/100)` 로 역산 (검산: 1386.2÷8.712 = 159.1 — 실제와 일치).

### 만들 것 (우선순위 제안)

| # | 기능 | 내용 | 데이터 | 난이도 |
|---|---|---|---|---|
| 1 | ~~ETF 괴리율~~ ✅ 08-21 | `/etf` 페이지 — 실시간 괴리(시세5초/NAV20초) + 네이버 기준 병기, 괴리 큰 순 정렬, 검색 추가 | — | — |
| 2 | ~~세션 타임라인 바~~ ✅ 08-21 | /etf 상단에 구현 (US장은 서머타임 동적 계산, 진행 중 세션 하이라이트) — 다른 페이지 이식 가능 | — | — |
| 3 | **김프 패널 보강** | 비교 기준 선택, 미결제약정·24h 거래대금 표시 | 이미 받는 데이터 | 하 |
| ~~7~~ | ~~**지수(`/idx`)**~~ ✅ 08-21 | 야간 선물 환산까지 확장 | — | — |
| ~~8~~ | ~~**시총비교(`/marketcap`)**~~ ✅ 08-21 | 종목 교체·우선주 합산·역전 탐지 | — | — |
| ~~9~~ | ~~**전고대비(`/peak`)**~~ ✅ 08-21 | 회복 필요 상승률까지 | — | — |
| 10 | **램값(`/ram`) 전용 페이지** | DRAM 시세 상세 + 반도체주 상관 | DRAMUSDT klines | 중 |
| 11 | **하닉ADR(`/adr`)** | ADR vs 국내 괴리 (김프의 ADR 버전) | SKHY.O + SKHYUSDT | 중 |
| 4 | **멀티 계좌** | /assets에 계좌 여러 개 (ISA/연금/일반 분리), 계좌별 합계 + 전체 합계 | localStorage 구조 확장 | 중 |
| 5 | ~~그리드 탭~~ ✅ 08-20 | 그리드 매매 트래커 — 완료. 사다리 시각화·원탭 체결·거래세 반영·APR | /assets 탭 | — |
| 6 | **가격 알림 데몬** | 관심종목 목표가/등락률/그리드 레벨 도달 시 macOS 알림 (wrap.js처럼 launchd, 장중 1분 폴링) | 기존 API + launchd | 중 |

### 안 만들 것

- ☁️ 동기화 서버 (프라이버시 — JSON 백업으로 대체)
- 접속자 수 표시 (개인용이라 무의미)
- Hyperliquid 제휴 링크 (걔네 수익모델)

## 진행 기록

- 2026-08-20: 인벤토리 작성. /calc → /assets ✓, 김프 패널 ✓
- 2026-08-20: 그리드 탭 완료 (계산 손검증: 매도 +12,945 = 3,000×5주 − 세 2,055 일치)
- 2026-08-21: ETF 괴리율 + 세션 타임라인 완료
- 2026-08-21: **터미널(`/terminal`) 완료** — 재무카드(PER·추정PER·PBR·외인·배당·시총·목표주가+30일 스파크라인) · 상대수익률 SVG 차트(1D/1M/3M/6M/1Y) · 지수·환율 레일(엔/달러 역산) · 글로벌 반도체 4종 · 바이낸스 24h 캔들차트(6심볼×5주기). 차트는 전부 인라인 SVG(의존성 제로 유지)

### 2026-08-21 — 지수 · 시총비교 · 전고대비 완료

세 페이지를 한 번에 붙이면서 공통 계층을 먼저 뽑았다.

**공통 모듈 신설**
- `public/chart.js` — SVG 차트 엔진(꺾은선·캔들·스파크라인 + 십자선 값 읽기). 라이브러리 없음.
- `public/chart.css` — 차트 표현 계층 + 공통 내비게이션 스타일
- `public/nav.js` — 페이지 8개의 내비게이션을 한 곳에서 렌더. 전에는 HTML 8곳에 하드코딩돼 있었다.
- `terminal.js` 263→217줄: 자체 SVG 코드를 걷어내고 `chart.js`로 통합

**서버 신설**
- `naverDaily()` — 네이버 siseJson 장기 일봉(코스피 1990년~, 9,453행). 토스 200개 상한을 우회하는 유일한 장기 소스. Referer 필수.
- `hlXyz()` — Hyperliquid xyz DEX 115종 시세. `/api/gap`과 캐시 공유.
- `/api/idx` `/api/hl/candles` `/api/marketcap` `/api/peak`

**지수 `/idx`** — 낮엔 KRX 현물, 밤엔 선물.
코스피 야간 추정 = 코스피 × (xyz:KR200 ÷ 코스피200 현물). 베이시스를 그대로 옮기는 방식이라
장중엔 실제 코스피와 나란히 놓고 검산된다. 거래량·미결제약정이 0인 HL 심볼(VIX·DXY·NIFTY 등)은
마크가 고정된 껍데기라 `stale`로 표시해 값으로 못 읽게 막았다.

**시총비교 `/marketcap`** — 종목 교체 가능, 우선주 합산 토글, 역전 이력 탐지.
실측 결과 2026-06-22에 SK하이닉스가 삼성전자를 65.6조 앞선 역전이 있었다.

**전고대비 `/peak`** — 낙폭과 **회복 필요 상승률**을 나란히. −50%는 +100%가 있어야 본전이라는 비대칭이 요점.
지수도 종목처럼 다룬다: 코스닥은 2000-03-10 고점(2,925.50) 대비 아직 −72.7%.

**작업 중 잡은 버그 3개**
1. `/api/terminal`의 원/달러 등락률 **부호 반전** — 네이버가 이미 음수로 주는데 방향으로 한 번 더 뒤집고 있었다.
   하락을 상승으로 표시하던 실제 오류. `signed()`로 통일.
2. 시총 장기 구간 **허수** — SK하이닉스 2002년 21:1 감자 탓에 수정주가 × 현재주식수가
   1999년 시총을 524조(당시 코스피 전체보다 큼)로 만들었다. 시총비교 기간 상한을 10년으로 제한.
3. 네이버 일봉 **결측 행** — 코스피 2007-03-02의 시/고/저가가 전부 0이라 전체 기간 최저가가 0이 되어
   '저점 대비'가 통째로 깨졌다. 0이면 종가로 메우도록 수정.

**남은 것**: 램값(`/ram`) · 하닉ADR(`/adr`) · 김프 패널 보강 · 멀티 계좌 · 가격 알림 데몬

### 2026-08-21 (2) — 남은 5개 전부 완료

**램값 `/ram`** — ⚠️ 조사 중 전제가 뒤집혔다.
`DRAMUSDT`는 램 칩 가격이 아니라 **티커 DRAM인 미국 상장 메모리 반도체 ETF**다. 실측 근거 3가지:
`exchangeInfo`의 `underlyingType = EQUITY`(금은 `COMMODITY`로 따로 있다) ·
`constituents`가 `dxfeed DRAM:USLF24`/`kaiko KK_RFR_DRAMUSD`/`pyth DRAM`로 마이크론과 동일한 미국주식 템플릿 ·
요일별 변동성이 평일 4.1~6.7% vs 토 1.31%/일 1.78%로 미국 증시 리듬.
→ 페이지를 "램값"이라 부르되 화면에서 정체를 근거와 함께 밝히고, 실제 쓸모(24시간 거래 → **국장 마감 이후 변화**)를 주인공으로 놓았다.
메모리주와의 상관·베타도 낸다(하이닉스 r=0.95 β=1.00, 엔비디아 r=0.57 β=0.19 — 메모리주와 로직주가 갈린다).

**하닉ADR `/adr`** — ADR 비율이 이 페이지의 급소다. 틀리면 프리미엄이 통째로 헛것이 된다.
어느 API도 비율을 직접 주지 않아 **네이버 ADR 시총 ÷ ADR가 ÷ 발행주식수 = 9.9777 → 10**으로 역산했고,
매 요청마다 다시 계산해 화면에 근거와 함께 노출한다(하드코딩 안 함).
검증: 네이버 ADR 시총(1,641조) vs 한국 실제 시총(1,279조) = +28.3%, 가격 직접 비교 +28.6% — 두 방법이 일치.
현재 ADR은 한국 대비 **+32% 프리미엄**. 이력 29거래일(7/30 +56.4% → 현재 +32%).
⚠️ 네이버 폴링이 직전 정규장 종가에 머무는 동안 토스·HL은 다음 세션을 반영한다 — 가장 신선한 값으로 헤드라인을 낸다.
⚠️ HL `xyz:SKHY`는 `markPx == oraclePx`라 독립 가격발견이 아니라 오라클 추종이다. 화면에 명시.

**김프 패널 보강** — 비교 기준 선택(자동/현재가/NXT/전일종가), 미결제약정·24h 거래대금, 데이터 신선도.
⚠️ **실제 버그 수정**: 펀딩비가 시간당 값이라 소수 2자리로는 전부 `−0.00%`로 찍히고 있었다.
연율로 환산하니 **−51%, −76%**(숏이 롱에 지급) — 보이지 않던 정보가 드러났다.

**멀티 계좌 `/assets`** — v1→v2 자동 마이그레이션(v1은 동결·백업, 무손실 검증 완료).
행 식별을 **고유 `hid`** 로 바꿨다 — 처음엔 `aid|id` 복합키를 썼는데 계좌를 지워 보유를 옮길 때
같은 종목이 겹치면 키가 충돌해 한 행을 고치면 다른 행이 바뀐다. 계좌 삭제 시 중복 종목은 차수·매도를 병합한다.
같이 고친 기존 버그 3개:
1. **5초 폴링이 커서를 파괴** — `refreshQuotes → render`가 무조건 목록을 재구성해 타이핑 중 커서가 날아갔다. activeElement 가드 추가.
2. **실현손익 인덱스 어긋남** — 필터된 배열을 원본 인덱스로 조회해, 미완성 매도 기록이 끼면 뒤 행 손익이 한 칸씩 밀렸다.
3. **export에 그리드 누락** — "백업했다"고 믿고 그리드 기록을 잃을 수 있었다.

**가격 알림 데몬** — `alert.js` + `alerts.json` + `/alerts` UI + launchd(60초).
브라우저와 무관하게 동작한다. 폭주 방지 3단: 교차 판정(첫 실행은 절대 발화 안 함) → 히스테리시스 → 쿨다운.
`osascript`는 `on run argv` 인자 전달을 쓴다 — wrap.js의 문자열 보간 방식은 메시지에 큰따옴표가 들어가면 죽는다(실측).
방해금지 모드에서 알림이 삼켜져도 소리는 나게 `afplay` 병행.
시세는 로컬 서버 우선 + 네이버 폴백(서버가 꺼져도 알림이 죽지 않게).
검증: 첫 실행 무발화 → 교차 시 발화 → 쿨다운 차단 → 그리드 라인 통과 → 서버 다운 시 폴백, 전부 통과.

**보안** — 쓰기 API가 생겨서 `server.listen`을 `127.0.0.1`로 축소했다(전엔 `*:8787`).
`/api/alerts`에 `req.method` 분기를 명시했다 — 이 서버는 그동안 메서드를 한 번도 보지 않아서
쓰기 라우트를 그냥 추가하면 GET으로도 설정이 덮어써진다.

**공통** — `Chart.timeAxis()` 추가. 간격 이름이 아니라 실제로 걸친 기간으로 라벨 형식을 정한다
(1시간봉 200개 = 8일치인데 시:분만 찍어 "04:00 → 22:00 → 17:00"처럼 거꾸로 읽히던 문제).

### 2026-08-21 (3) — 적대적 리뷰와 그 결과

5개 차원(정확성·데이터손실·데몬신뢰성·보안·화면)으로 나눠 코드를 다시 뜯고,
나온 지적마다 "이 지적이 틀렸다"를 기본값으로 두고 반증을 시도했다.
47건 중 **43건이 반증에 실패**(= 진짜 문제)했고 4건은 반증됐다. HIGH 5건은 전부 수정했다.

**HIGH — 전부 내가 이번에 만든 것에 있던 문제**

1. **장후 알림이 구조적으로 0건이었다** (`alert.js`) — NXT 창(15:35~20:00)에 데몬은 계속 도는데
   판정은 15:30에 얼어붙은 정규장 종가로 했다. `extPrice`를 파싱해두고 한 번도 쓰지 않았다.
   → `priceOf(q, win)` 도입. NXT 창에서는 NXT 가격으로 판정한다.
2. **갭 모드가 방아쇠를 소진했다** (`alert.js`) — 평가 함수가 발화 판단과 동시에 armed·쿨다운을
   기록했는데 호출부가 알림만 버렸다. 신호 하나를 놓치는 게 아니라 그 뒤 60분을 침묵시켰다.
   → 상태 갱신과 발화 판정을 분리(`record` 모드). 기준만 기록하고 방아쇠는 안 건드린다.
3. **가져오기가 그리드를 전멸시켰다** (`assets.js`) — 내보내기는 그리드가 0개일 때도 `grids: []`를 넣는다.
   그 파일을 몇 달 뒤 가져오면 빈 배열이 유효값으로 통과해 체결 기록을 지웠고, 확인창은 그리드를
   언급조차 하지 않았다. → 증감을 확인창에 명시 + 덮어쓰기 전 별도 백업.
4. **가져오기 후 메모리 미갱신** (`assets.js`) — 복구 직후 그리드 조작 한 번이면 옛 배열이 되살아나
   복구분이 날아갔다. → `grids = loadGrids(); renderGrids();`
5. **깨진 v2를 백업 없이 덮어썼다** (`assets.js`) — `assets-v2`가 한 글자만 손상돼도 조용히 v1 시절로
   되감기고, 부팅 직후 save()가 깨진 원본까지 덮어썼다. 한 글자 고치면 살릴 수 있던 손상이
   영구 소실이 됐다. → 손상본을 `assets-v2-broken-<시각>`에 보존 + 화면 배너로 고지.

**운영 중 실제로 터진 것**: 테스트로 넣은 잘못된 설정 하나 때문에 데몬이 99% CPU로 **1시간 51분** 돌았고,
launchd는 실행 중 인스턴스가 있으면 다음을 안 띄우므로 그동안 알림이 완전히 죽어 있었다. 증상은 침묵뿐이었다.
→ 45초 워치독 추가. 무슨 일이 있어도 다음 실행 자리를 비운다.

**MEDIUM 주요 수정**: `once:true`가 전혀 동작 안 함(설정 플래그와 상태 플래그가 엇갈림) ·
targets에 null 하나면 데몬 전체 사망 · 자정 롤오버가 그리드 lastLine 미초기화 ·
매일 아침 첫 회차가 항상 갭 모드 · 시스템 타임존 의존 · `__proto__` 프로토타입 오염 ·
중복 id가 상태 공유 · ADR 이력이 라이브와 다른 한국 종가 기준(토스 NXT 포함 vs 네이버 정규장) ·
밤사이 기준봉 15분 밀림 + 주말 무시 · 매도 > 매수 시 음수가 합계 오염 · APR이 '입력한 날' 기준.

**전부 완료.** 남은 항목 없음.


### 2026-08-21 (4) — 프론트 배치 정리

전 페이지를 1440/1024/768/390px 에서 실측하고 고쳤다.

**내비게이션이 가장 큰 문제였다.** 페이지가 11개가 되면서 테두리 칩 11개가
상단바 폭의 **67%(828px)** 를 먹고, 좁아지면 2~3줄로 접혀 모바일 상단바만 **160px** 이었다.
- `flex-wrap: wrap` → `nowrap` + 가로 스크롤. 어떤 폭에서도 한 줄이다.
- 비활성 링크는 테두리를 빼고 글자만 남겼다. 지금 있는 페이지만 칩.
- 오른쪽 끝 마스크 그라디언트로 "더 있다"를 알리고, 현재 페이지가 스크롤 밖이면 끌어온다.
- 결과: 모바일 헤더 **160px → 92px**, 1024px 에서 76 → 56px.

**그 밖에**
- `/flow` 만 자체 헤더라 내비가 2행에 있었다 → 제목 줄로 올리고 여백을 `.topbar` 와 맞춤(페이지를 오갈 때 본문이 위아래로 튀지 않게).
- 차트 끝점 라벨이 두 계열이 수렴하면 겹쳤다 → 충돌 시 위아래로 벌리고 인도선으로 실제 끝점과 연결. 비교 차트는 선이 만나는 게 핵심이라 반드시 필요한 처리.
- 축 날짜가 `ko-KR` 로케일 때문에 `"08. 11."`·`"26. 8."` 처럼 꼬리 점과 공백이 붙었다 → 직접 조립(`08.11`). `Chart.md()` 로 공용화.
- 갱신 시각이 `"17시 28분 56초"` 였고 문구도 페이지마다 '마지막 갱신'/'갱신' 으로 갈렸다 → `en-GB`(`17:28:56`) + '갱신' 으로 통일.
- 관심종목 표가 375px 에서 457px 로 넘쳐 등락률이 안 보였다(가용 301px) → 520px 이하에서 등락(원)·장상태 배지를 접고 패널 여백을 줄여 **정확히 들어가게** 함. 등락률은 끝까지 지킨다.

전 페이지 재검증: 1440/1024/768/390 전부 가로 넘침 0, 내비 1행, JS 오류 0.

### 2026-08-24 — ETF 괴리율이 통째로 틀렸던 것

`/etf` 의 존재 이유인 괴리율이 **최대 30배 틀린 값**을 보여주고 있었다.
UX 검토 중 화면을 눈으로 보다가 우리 값(−15.15%)과 네이버 값(−0.47%)이 너무 달라서 파봤다.

**원인**: 네이버 `etfKeyIndicator.nav` 는 실시간 iNAV 가 아니라 **전일 기준 NAV** 다.
그걸 현재가와 비교하면, 그날 많이 움직인 종목의 괴리가 '그날 등락률' 만큼 통째로 오염된다.
- 실측: 삼성전자 −6.8% 인 날, 2배 레버리지 ETF 가 우리 −15.15% vs 실제 −0.47%
- 7개 종목 전부 `nav` 가 `현재가 ÷ (1+등락률)`(=전일 종가) 과 2% 이내로 일치 → 전일값 확정
- 반대로 `deviationRate` 는 실시간이다(가격을 따라 움직이는 것을 확인)

**수정**: 실시간 iNAV 를 괴리에서 역산한다 — `iNAV = 현재가 ÷ (1 + 괴리)`.
네이버 `deviationRate` 를 주인공 값으로 쓰고, 전일 NAV 는 맨 오른쪽 참고 열로 내렸다.
화면 설명에도 왜 역산하는지 적었다.

**왜 오래 안 걸렸나**: 조용한 날엔 오차가 작다(하이닉스 −0.56% 인 날 −1.20% vs −0.65%).
예전에 같은 증상을 'NAV 캐시가 낡아서' 로 오진하고 캐시를 2분→20초로 줄인 적이 있는데,
원인은 신선도가 아니라 **애초에 다른 값** 이었다. 캐시를 아무리 줄여도 안 고쳐졌을 문제.

### 2026-08-24 (2) — 시스템 테마(라이트/다크) 대응

`prefers-color-scheme` 으로 시스템 설정을 따라간다. 토큰 값만 갈아끼우고
컴포넌트 CSS 는 건드리지 않아, 각 페이지가 `var(--…)` 를 쓰기만 하면 자동으로 따라온다.

**색을 반전하지 않고 다시 계산했다.** 다크에서 잘 보이는 채도색은 흰 배경에서 대비가 무너진다.
- `#f04452`(상승) 는 흰 배경에서 3.71 → 라이트 전용 `#c02535`(5.9)
- `#ffd166`(강조) 는 흰 배경에서 **1.4** 라 글자색으로 못 쓴다 → `#8a6100`(5.5)
- 이 화면은 10.8px 글씨가 60~70% 라 대비를 양보할 여지가 없다

**두 번 틀리고 고친 것**
1. 처음엔 '흰 카드 위' 기준으로만 색을 골랐다. 그런데 내비·브랜드·배지는 **페이지 배경
   (`#eef0f4`) 위**에 놓여서 더 어둡다. `--text-faint` 를 `#6b7280`(배경 위 4.24)에서
   `#656c78`(4.64)로 내렸다. 두 표면 모두에서 4.5 를 넘어야 한다.
2. 틴트 배경 위에 같은 계열 글자를 얹는 곳(ETF 괴리 배지, 국내/미국 배지)은 또 다르다.
   흰 배경만 보고 고르면 배지에서 4.26 으로 떨어진다 → `--on-up-tint` / `--on-down-tint` 분리.

**측정 자체를 두 번 틀렸다 (기록해 둔다)**
- 리프 요소만 세어서 `.tc-price` 처럼 자식을 품은 큰 숫자를 통째로 놓쳤다 → 텍스트 노드 기준으로 교체
- 반투명 배경의 알파를 무시하고 불투명색으로 대비를 계산해, 멀쩡한 배지를 1.31 로 오판했다
  → 조상까지 올라가며 알파 합성 후 계산. 오탐 13건이 5건으로 줄었다

**하드코딩 정리** — 라이트 대응의 절반은 이 작업이었다
- `#3fb950`(정상/장중) 이 7개 파일에 흩어져 있던 것을 `--ok` 로
- 흰색·검정을 직접 섞던 15곳(`rgba(255,255,255,.03)` 등)을 `--tint-hover/active` 로
- 다크 전용 밝은 색(`#ff8a93`, `#7eb3ff`, `#3a4560` …)을 모드별 토큰으로
- JS 가 하드코딩하던 차트 계열색을 `Chart.series()` / `Chart.token()` 으로 (CSS 변수를 읽는다)

**자책 기록**: `rgba(63,185,80,…)` 일괄 치환이 **토큰 정의 자체까지** 바꿔
`--ok-tint: var(--ok-tint)` 자기 참조를 만들었다. 그 뒤 토큰들이 통째로 누락됐고
저장 버튼이 대비 1.57 로 깨졌다. sed 로 색을 일괄 치환할 때는 정의부를 반드시 제외할 것.

**곁다리로 고친 것** (검토에서 확정된 건)
- `/terminal` 삼성전자 카드 띠가 `--accent`(앰버)여서 차트 선(민트)과 어긋났다 → `--series-1`
- `.q-ext` 의 `opacity: 0.85` 가 대비를 계산 없이 4.87→4.42 로 깎았다 → opacity 제거
- 관심종목 선택 행 틴트 0.05 가 파랑 대비를 4.42 로 깎았다 → 0.035

**검증**: 11페이지 × 2모드, 알파 합성까지 반영한 대비 계산으로 **미달 0건**.

### 2026-08-24 (3) — 테마 수동 토글

시스템 추종만으로는 부족해서 버튼을 붙였다. **3단 순환: 시스템 → 라이트 → 다크 → 시스템.**
'시스템'을 없애고 둘만 두면 OS 를 바꿔도 대시보드가 안 따라와 사용자가 두 곳을 관리하게 된다.

**CSS 구조가 핵심** — `@media` 만 있으면 수동 선택이 OS 를 못 이긴다. 세 겹으로 쌓았다:
```
:root                                             다크 (기본)
@media (light) { :root:not([data-theme="dark"]) } OS 라이트 — 단 사용자가 다크를 골랐으면 양보
:root[data-theme="light"]                         사용자가 라이트를 고름 — OS 가 다크여도 이김
```
`:not([data-theme="dark"])` 가드가 없으면 "OS 라이트 + 다크 선택" 조합이 깨진다.

**깜빡임 방지** — nav.js 는 body 끝에서 돌아 이미 한 번 그려진 뒤다.
11개 HTML `<head>` 의 tokens.css 링크 앞에 인라인 스크립트를 넣어 페인트 전에 적용한다.

**버튼은 nav.js 가 만든다** — 11개 HTML 을 고칠 필요 없이 `.status` 앞에 붙는다.
내비 스크롤 영역 **밖**에 둔 이유: 안에 넣으면 링크 11개에 밀려 안 보인다.
아이콘만 쓰고 상태는 title/aria-label 로 말한다(상단바 가로 예산이 이미 빠듯).
'시스템'일 때는 지금 실제로 어느 쪽인지도 툴팁에 같이 보여준다.

**검증**: OS 다크/라이트 × 시스템·라이트·다크 = 6조합 전부 통과.
새로고침 유지 ✓ · 11페이지 전체 유지 ✓ · 상단바 높이 데스크톱 44px 그대로(모바일 92→103px).

**측정 함정 하나 더 기록** — 브라우저 창이 숨겨져 있으면 Chrome 이 스타일 재계산을 미룬다.
`data-theme` 을 바꾸고 곧바로 `getComputedStyle` 을 읽으면 **한 단계 이전 색**이 나온다
(`--text-faint` 는 새 값인데 `color` 는 옛 값). rAF 도 안 돈다.
CSS 를 의심하기 전에 창이 보이는지, 혹은 새로고침 같은 정적 경로로 재검증할 것.

### 2026-08-27 — 상태 처리 (검토 1순위)

UX 검토 28건 중 **상태 처리가 12건**으로 압도적이었다. 개별 버그가 아니라
"데이터가 언제 것인지, 지금 유효한지를 화면이 밝히지 않는다"는 **한 가지 결함이 12곳에 반복**된 것.
이 대시보드는 장외에 더 자주 열리기 때문에 이게 가장 값비싼 결함이다.

**HIGH 2건**
1. `/etf` 세션 타임라인이 요일만 보고 판정해 **공휴일에 "KR 정규 ● 진행 중"이라고 거짓말**했다.
   1년에 10일 넘게 정반대를 말하는 셈. 거래소 기준을 아는 건 서버뿐이라
   `/api/etf` 응답에 `marketState` 를 실어 진실의 원천으로 삼았다.
   모를 때(null)는 시간대 추정으로 물러난다 — 거짓 단정보다 낫다.
   미국 세션엔 적용하지 않는다(한국 휴장일에도 미국은 열린다).
2. `/marketcap` 데이터 실패 시 **이전 종목 차트가 그대로 남아** 새 종목 것처럼 보였다.
   칩은 이미 새 이름으로 바뀐 뒤라 "현대차를 골랐는데 삼성전자 곡선을 현대차로 믿는" 상황.
   화면 대부분이 차트라 작은 오류 문구는 그걸 이기지 못한다 → 차트·범례·노트를 함께 비운다.

**공통 처리로 승격**
- `Chart.asOf()` / `Chart.asOfBadge()` — 장 상태 판정과 문구를 한 곳에서. 페이지마다 만들면 같은 상태를 다르게 말하게 된다.
- `body.stale` — flow 만 쓰던 패턴을 전 페이지로. 다만 **흐림만으로는 경고가 안 읽혀서**
  상단 문구도 `'갱신 실패 — 아래 값은 이전 것'` 으로 바꿨다(11페이지 통일).
- `.chart-box > .inv-empty` 세로 가운데 정렬 — 한 줄로 6개 페이지의 "빈 상자 위쪽에 붙은 문구"가 고쳐진다.

**개별**
- `/terminal` 종목 카드·`/idx` 해외 지수에 장 상태 배지 (주말 종가를 현재가로 읽던 것)
- `/assets` 총 평가금에 '종가 기준'/'장중' 표시 + 요약에 '기준 시점' 행
- 6개 페이지에 first-load 가드 — 첫 로드부터 실패하면 스켈레톤이 영원히 반짝이고 재시도 수단도 없었다
- `/peak` 누락 종목 이름 표시 · 선택 종목이 응답에서 사라지면 상세 차트 닫기 · 빈 목록 시 요약 초기화
- `/etf` 전부 삭제 후 새로고침하면 기본 7종이 부활하던 것 — 빈 배열도 존중
- `/alerts` 로드 실패 시 그리드 패널도 함께 알림 · `/terminal` 우측 레일 스켈레톤

**자책 기록(두 번째)** — 이스케이프를 정리한다고 `\"` → `"` 를 **전역 치환**해서 6개 파일을 깨뜨렸다.
백틱으로 열고 작은따옴표로 닫힌 문자열 15곳이 생겨 파싱이 무너졌다.
`node --check` 로 즉시 잡아 복구했지만, **일괄 치환은 범위를 좁히고 반드시 문법 검사를 붙일 것.**
(8/24 의 `rgba()` 토큰 자기참조 사고와 같은 유형 — 두 번째다.)

**검증**: 11페이지 JS 오류 0 · 스켈레톤 잔존 0 · 서버 로그 오류 0.
`/marketcap` 실패 재현 시 차트선 2→0, 범례·노트 비워짐, 재시도 버튼 생성, 상단 문구 전환 확인.

---

## 2026-08-29 — 정확성·서식 검토 37건 수정, 그리고 꾸미기

### 배경
UX 검토를 6조각으로 쪼개 돌렸다(한 에이전트가 11페이지를 통째로 맡으면 오래 걸려 죽는다).
**63건 제기 → 58건 검증 → 21건 반증 기각 → 37건 확정.** 아래는 그 37건과 후속 작업.

### ① 화면이 거짓말하던 것

- **`/assets` "장중 실시간" 배지** — 통화를 한 뭉치로 묶어 판정해서, 미국장만 열린 새벽에도
  원화 합계(금요일 종가) 옆에 '장중'이 붙었다. 통화별로 갈라 판정하고 '기준 시점'도 둘 다 적는다.
- **`/assets` 그리드 · `/alerts` · `alert.js`** — 미국 종목을 넣으면 값이 전부 '원'으로 찍히고
  센트가 반올림돼 사라졌다($209.66 → "210원"). 통화는 `id` 접두사로 알 수 있다 — 별도 필드 없이 갈랐다.
  알림 데몬 문구도 같이 고쳤다(목표가와 현재가의 단위가 달라 비교 자체가 안 됐다).
- **`/ram` "국장 마감 이후"** — 요일만 보고 토·일을 건너뛰어서 **공휴일이 그대로 통과**했다.
  공휴일 달력을 들고 다니는 대신, 휴장일엔 행 자체가 없는 네이버 일봉을 KRX 영업일 달력으로 썼다.
- **`/` 카드 뷰** — 시총·PER·상승여력이 프로필 캐시(30분) 시점에 고정돼, 5초마다 갱신되는
  현재가와 다른 시점을 나란히 보여줬다. 주당값은 장중에 안 변하므로 가격비만 곱해 같은 시점으로 맞춘다.
- **`/adr`** — 한 화면에 '현재 프리미엄'이 둘(실시간 vs 일별 종가)이었다. 무엇끼리 비교한 값인지 명시.
  두 값이 우연히 같아지는 장 마감 시간엔 "다릅니다"라는 문구 자체가 거짓이 되므로 조건부로 바꿨다.
- **`/marketcap`** — '기간 최대 격차' 라벨이 "슬롯 A가 더 큰 회사"라는 가정에 기대고 있었다.
  슬롯을 바꾸면 뜻이 뒤집힌다 → 실제 회사 이름을 라벨에 넣는다. 장중/마감 배지도 추가(서버는 주던 값).
- **`/flow` 스캐너** — 최초 1회만 그려져 헤더 시각만 갱신됐다. 등락 출처(토스 일봉)도 헤더에 밝혔다.

### ② 색이 두 뜻이던 것
같은 화면에서 빨강/파랑이 '등락'과 '다른 무엇'을 동시에 뜻하면 둘 다 안 읽힌다.
- 김프 배지 · 목표가 막대 · `/idx` 그날 고·저 · `/marketcap` 역전 폭 → 등락색 제거(중립 강조색 또는 무채색)
- `/etf` 세션 바 → 지역을 색으로 가르던 것을 빗금으로. `opacity` 로 흐리면 글자까지 흐려진다
- 하드코딩된 `rgba()` 틴트 → 토큰 (라이트에서 안 따라오던 것들)

### ③ 차트 좌표계 (가장 큰 수정)
`viewBox 1000 + preserveAspectRatio="none"` 으로 가로만 늘리고 있었다. **`<text>`도 같이 늘어난다.**
좁은 칼럼(223px)에서는 0.37배로 눌리고 넓은 화면에서는 1.37배로 퍼져서,
같은 `.axis-label` 이 페이지마다 다른 글자로 보였다. 끝점 태그의 알약 배경도 글자와 어긋났다.
→ viewBox 를 **컨테이너의 실제 픽셀 크기**로 잡아 1:1 로 만들고, 폭이 바뀌면 ResizeObserver 로 다시 그린다.
음수 부호(`-` vs `−`)와 눈금의 `−0.0%`(부동소수 오차)도 한 곳에서 통일했다.

**자책 기록** — 처음엔 폭 하한을 320px 로 뒀는데, 실제 223px 칼럼에서 **왜곡이 그대로 재발**했다.
하한은 좌표계가 깨지지 않을 최소치(PAD 합 74px)만 막으면 된다.
또 테마 변경 신호를 `requestAnimationFrame` 으로 미뤘는데, **탭이 숨으면 rAF 가 안 돌아** 신호가 영영 안 갔다.
`getComputedStyle` 로 스타일 재계산을 강제한 뒤 그 자리에서 쏘는 방식으로 바꿨다.

### ④ 꾸미기

**카드가 배경에 묻히던 문제** — 실측하니 bg~panel 색차가 ΔE 4.7(나란히 놓아야 겨우 보이는 수준)이었다.
판을 밝히면 그 위 10.8px 글씨의 대비가 같이 내려간다 → **판이 아니라 바닥을 낮췄다**(ΔE 4.7 → 7.2).
나머지 단차는 테두리와 그림자가 맡는다. 어두운 화면에서 '떠 있음'을 만드는 건 채움색이 아니라 가장자리다.

**타입 스케일** — 하드코딩 `rem` 리터럴 121곳을 토큰으로. 1.05rem 과 1.45rem 사이가 비어 있어서
리터럴 8개가 그 틈에 몰려 있었다 → `--fs-hl: 1.2rem`(18px) 추가.
페이지 제목 18 · 섹션 제목 15.75 · 본문 13 · 캡션 10.8 로 네 단을 벌렸다(전엔 세 단이 2.75px 안에 몰려 있었다).
설명 문장 20곳은 10.8px 바닥선에서 12px 로 — 바닥선은 배지용이지 문장용이 아니다.

**`/peak` 위계 뒤집기** — 낙폭이 페이지 제목이자 정렬 기준인데 현재가(15.75px)보다 작았다(13px).
낙폭을 21.75px 로, 현재가를 13px 로. 덤으로 18px 이상 글자가 0개이던 `/peak` `/etf` `/alerts` 문제도 풀렸다.

**호버** — 규칙 하나: **눌리는 것만 떠오른다.** 안 눌리는 카드가 손끝을 따라 떠오르면 거짓말이 된다.
① 카드형 클릭 대상(`.scard` `.peak-row`) 2px 들어올림 + 그림자 + `:active` 눌림
② 행형 클릭 대상(`.quotes tbody tr` `.h-row`) 배경 + 왼쪽 세로 막대 (행만 뜨면 줄이 어긋나 보인다)
③ 정보 전용 8종 테두리만 한 단 — `prefers-reduced-motion` 이면 이동은 끈다

**대비 재검증에서 나온 것** — 판을 조정하니 세 곳이 4.46~4.48 로 4.5 밑을 스쳤다.
`--text-faint` 를 한 단 밝히고(틴트 위에서도 4.65), `--panel-2` 는 한 칸 되돌리고,
호가창은 판 색 대신 틴트 위 전용 색(`--on-*-tint`)으로 바꿔 7.4 까지 올렸다.

### ⑤ 알림 데몬이 죽어 있던 것
`com.jk.stock-alert` 가 **launchd 에 아예 등록돼 있지 않았다**(8/27 오후 이후 무음).
데몬 자신의 설계 원칙 2("침묵의 원인을 구분할 수 있어야 한다") 덕분에 `/alerts` 의
'마지막 실행 3159분 전'으로 발견했다. 재등록 후 60초 주기 발화 확인.

### 검증
- 11페이지 × 2모드 × (1400px·390px): **대비 미달 0 · 가로 넘침 0 · JS 오류 0**
- 차트 viewBox 가 컨테이너 픽셀과 정확히 일치(785×300, 358×210, 223×300)
- 미국 종목 그리드 실측: 사다리·체결·통계 전 항목이 `$` 서식, 센트 보존
- 데몬 재등록 후 launchd 가 실제로 깨우는지 상태 파일 타임스탬프로 확인

### 2026-09-02 — 내비 이모지 → 선 SVG 아이콘
이모지는 자기 색을 고집해서 회색 링크 줄에서 혼자 총천연색으로 튀고, OS마다 모양이 달라 통제가 안 된다.
11개 탭 + 브랜드 마크 + 테마 토글을 stroke SVG(`currentColor`)로 교체 — 비활성(회색)·현재 페이지(강조색)·
라이트/다크가 전부 글자색을 따라온다. 아이콘은 nav.js 의 ICO 맵 한 곳에서 관리.

### 2026-09-02 — 카드↔표 전환 버그 (hidden 속성이 display 규칙에 진다)
사용자 제보: 카드로 갔다가 표로 돌아오면 카드가 그대로 남는다.
원인은 JS 가 아니라 CSS 우선순위 — `hidden` 속성의 브라우저 기본 `display:none` 은
`.card-grid { display: grid }` 같은 평범한 규칙 하나에 진다. 그래서 숨기는 방향만 실패했다.
같은 지뢰를 전수 조사: `.save-bar { display:flex }` 도 걸려 있었다(알림 저장 바가 항상 떠 있던 셈).
`[hidden] { display: none !important; }` 를 tokens.css 전역에 못 박아 한 번에 해결.
검증: 표→카드→표 왕복 2회 정상, 알림 저장 바 idle 숨김·편집 시 표시 확인.

### 2026-09-02 — 호가창 좀비 인터벌 (검토 15건 중 HIGH 1건, 직접 검증 후 수정)
종목을 빠르게 두 번 바꾸면 첫 `renderOrderbookTab` 이 `await draw()` 에서 자다가 깨어나
**지나간 종목의** 인터벌을 `obTimer` 에 덮어썼다. 앞 인터벌은 참조를 잃어 영원히 살고(좀비),
2초 뒤 "내 종목이 아니네" 하며 `stopOrderbookPolling()` 을 불러 **현재 종목의 타이머**를 죽였다.
증상은 '호가가 조용히 멈춤' — 오류가 없어 원인을 알 수 없는 유형.
→ 세대 번호(`obSeq`)로 지나간 호출은 타이머를 걸지도, 남의 타이머를 끄지도 못하게 했다.
검증: setInterval 을 계측한 뒤 6종목 연속 전환 → 살아있는 인터벌 1개, 폴링 유지, 선택 종목 일치.
(토큰 절약을 위해 워크플로 재실행 대신 제기된 15건을 직접 검증하는 방식으로 전환. 나머지 14건 미검증.)

### 2026-09-02 — 깨진 localStorage 가 페이지를 즉사시키던 것 (검토 15건 중 #2·#3)
`peak-codes-v1` · `mcap-picks-v1` 를 모듈 최상위에서 맨몸 `JSON.parse` 하고 있었다.
손으로 고치다 깨진 JSON 하나면 스크립트 전체가 던져서 핸들러 하나 안 걸린 빈 화면이 됐다
(같은 파일의 watchlist 읽기는 try 로 감싸놓고 정작 자기 키는 안 감싼 비일관).
→ 파싱 실패·형태 불일치(비배열, 잘못된 코드 형식)를 전부 기본값으로 되돌리는 `readCodes()`/`readPicks()`.
빈 배열은 '다 지운 상태'로 존중한다 — 기본값으로 되살리면 삭제가 안 먹는 버그가 된다.
검증: 두 키에 깨진 JSON·잘못된 배열을 넣고 로드 → 오류 0, /peak 5행·/marketcap 선 2개로 기본값 복구.

### 2026-09-02 — 검토 #1·#4·#7 (직접 검증 후 수정)
- **#7** `/idx` `/ram` 의 '다시 시도' 버튼이 존재하지 않는 `load()` 를 불러 무반응이었다. 페이지마다 진입 함수
  이름이 달라 생긴 일 — 여섯 페이지가 같은 이름 `load()` 를 갖도록 통일. (실측: 두 페이지 모두 `typeof load === 'function'`)
- **#4** `/assets` v1→v2 마이그레이션의 백업 `setItem` 만 try 밖이었다. 용량 초과면 `load()` 가 모듈 최상위에서
  던져 매 로드마다 죽고 마이그레이션이 영영 안 끝난다 → 백업 실패는 경고만 남기고 계속 진행.
- **#1** `watchlist-v1` 에 구버전 문자열 항목이 섞이면 `w.id` 가 undefined 라 대시보드·세력좌표가 던졌다.
  `peak.js` 는 이미 처리하고 있었는데 원본 소유자인 `app.js` `flow.js` 가 안 했다 → 두 곳 모두 정규화.
  (실측: 문자열·쓰레기 항목 섞은 목록으로 로드 → 오류 0, 유효 3종목만 남음, /flow 스캔 정상)

### 2026-09-02 — 검토 #8·#9·#11 (경쟁 조건 두 곳 + TTL 미발동)
- **#8** 뉴스/랭킹, **#11** 지수 차트: 탭을 빠르게 바꾸면 느린 옛 응답이 나중에 도착해 새 탭 내용을 덮어썼다
  (도착 순서가 이김). 요청 번호를 매기고 돌아왔을 때 최신이 아니면 버린다.
  실측: KOSPI→KOSDAQ→KPI200 연속 전환 후 차트 끝값 1,031.53 = 코스피200 현재값 · 랭킹→뉴스 연속 전환 후 랭킹 행 0.
- **#9** 프로필 10분 TTL 이 어디서도 발동하지 않았다 — `loadProfiles()` 가 뷰 전환·첫 로드에서만 불렸다.
  주기 시세 갱신에 얹었다(만료된 게 없으면 즉시 false 라 비용 없음). 실측: 만료시킨 뒤 주기 경로에서 재조회 확인.
